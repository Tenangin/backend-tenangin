[functions]
  directory = "netlify/functions"
  external_node_modules = ["express", "serverless-http", "cors", "express-session", "dotenv", "passport", "jsonwebtoken"]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true